# 🔧 ВАЖЛИВО: Налаштування на Render.com UI

## ⚠️ Проблема

Render не читає `render.yaml` при звичайному деплоймету через UI. Потрібно **вручну встановити** Start Command.

## ✅ Рішення - Виконайте ці кроки:

### 1. Перейдіть на [Render Dashboard](https://dashboard.render.com)

### 2. Знайдіть ваш Web Service `remoteorder-server`

### 3. Натисніть на **Settings** (або gear icon)

### 4. Прокрутіть до розділу **Build & Deploy**

### 5. Знайдіть поле **Start Command** 

**Замініть його ТОЧНО на:**
```
npx prisma migrate deploy && node dist/index.js
```

⚠️ **ВАЖЛИВО**: Не використовуйте `npm start` або посилання на package.json!

### 6. Натисніть **Save**

### 7. Натисніть **"Deploy latest commit"** (або Manual Deploy)

---

## 🔍 Перевірка

1. Слідкуйте у **Logs** за повідомлення:
   ```
   🚀 Server is running on port 10000
   ```

2. Перевірте здоров'я:
   ```bash
   curl https://remoteorder-server.onrender.com/health
   ```

3. Якщо бачите `"✅ PostgreSQL Connected via Prisma"` — все готово!

---

## 📸 Скріншот (мітка де встановлювати)

```
Web Service: remoteorder-server
│
├── Environment
│   ├── DATABASE_URL = postgresql://...
│   └── NODE_ENV = production
│
├── Build Command
│   └── npm install && npx prisma generate && npm run build
│
└── ⭐ Start Command ⭐
    └── npx prisma migrate deploy && node dist/index.js
        ↑ ВСТАНОВІТЬ САМЕ ЦЕ
```

---

## 🆘 Якщо все ще не працює

### Варіант A: Скопіюйте ТОЧНИЙ Start Command
```
npx prisma migrate deploy && node dist/index.js
```

Без npm, без shell обгортки - саме так!

### Варіант B: Пересвідчитесь у логах
- Натисніть на Web Service
- Перейдіть до **Logs**
- Подивіться останні повідомлення про помилки

### Варіант C: Перезавантажте сервіс
- Settings → Redeploy → Manual Deploy

### Варіант D: Перевірте package.json
```json
{
  "scripts": {
    "start": "node dist/index.js"
  }
}
```
Він має бути саме такий!

---

## 📝 Нотатка про render.yaml

`render.yaml` у репозиторії потрібен для **Infrastructure as Code** деплоймену через `render up`.
Для звичайного GitHub деплоймету через UI - встановлюйте команди вручну в Render Dashboard.

---

**Після встановлення Start Command - деплоймент повинен працювати! ✅**
